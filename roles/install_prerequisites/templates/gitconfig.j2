[credential]
  helper = store 
[user]
  email = deadkid@gmail.com
  name = Scott Bennett
[diff]
  external = difft
[core]
  longpaths = true
  autocrlf = false  
[push]
  autoSetupRemote = true
[filter "lfs"]
        process = git-lfs filter-process
        required = true
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
