#!/bin/bash

if [[ -z "${WSL_DISTRO_NAME}" ]]; then
   alias wsl="figlet 'You are already in WSL.'; figlet 'You big dummy!'"
fi

alias desktop="cd $USERPROFILE/Desktop/"
alias downloads="cd $USERPROFILE/Downloads/"
alias dl="downloads"
